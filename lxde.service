[Unit]
Description=LXDE service

Before=wm.target
After=xsettings.target
After=xorg-base.target

Requires=xorg-base.target

Conflicts=awesome.service
Conflicts=openbox.service
Conflicts=stumpwm.service

[Service]
ExecStart=/usr/bin/lxsession -s LXDE -e LXDE
# ExecStop=/usr/bin/openbox --exit
Restart=on-failure
RestartSec=1

[Install]
WantedBy=wm.target
